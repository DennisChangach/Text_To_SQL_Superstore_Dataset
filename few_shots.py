few_shots = [
    {'Question' : "What is the total sales amount for all orders?",
     'SQLQuery' : "SELECT SUM(Sales) FROM orders",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "How many orders were returned in total?",
     'SQLQuery' : "SELECT COUNT(DISTINCT Order_ID) FROM returns",
     'SQLResult': "Result of the SQL query",
     'Answer': "Answer"},
    {'Question' : "Who is the regional manager for the region with the highest sales?",
     'SQLQuery' : "SELECT p.Regional_Manager FROM people p JOIN (SELECT Region, SUM(Sales) AS TotalSales FROM orders GROUP BY Region ORDER BY TotalSales DESC LIMIT 1) AS max_sales ON p.Region = max_sales.Region",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Sadie Pawthorne"},
    {'Question' : "What is the total profit earned?",
     'SQLQuery' : "SELECT SUM(Profit) FROM orders",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "How many orders were placed in total?",
     'SQLQuery' : "SELECT COUNT(DISTINCT Order_ID) FROM orders",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "What is the average discount rate for all orders?",
     'SQLQuery' : "SELECT AVG(Discount) FROM orders",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "How many orders were shipped via standard mode?",
     'SQLQuery' : "SELECT COUNT(DISTINCT Order_ID) FROM orders WHERE Ship_Mode = 'Standard'",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "What is the total quantity of products sold?",
     'SQLQuery' : "SELECT SUM(Quantity) FROM orders",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "What is the average sales per order?",
     'SQLQuery' : "SELECT SUM(Sales)/COUNT(DISTINCT Order_ID) FROM orders",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "How many unique customers placed orders?",
     'SQLQuery' : "SELECT COUNT(DISTINCT Customer_ID) FROM orders",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
     {'Question' : "What are the top 5 product categories by total sales?",
     'SQLQuery' : "SELECT Category, SUM(Sales) AS TotalSales FROM orders GROUP BY Category ORDER BY TotalSales DESC LIMIT 5",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "Which city has the highest total sales?",
     'SQLQuery' : "SELECT City, SUM(Sales) AS TotalSales FROM orders GROUP BY City ORDER BY TotalSales DESC LIMIT 1",
     'SQLResult': "Result of the SQL query",
     'Answer': "Answer"},
    {'Question' : "How many orders were returned in each country?",
     'SQLQuery' : "SELECT o.Country, COUNT(DISTINCT r.Order_ID) AS TotalReturns FROM returns r JOIN orders o ON r.Order_ID = o.Order_ID GROUP BY o.Country",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "What is the average profit margin for each product sub-category?",
     'SQLQuery' : "SELECT Sub_Category, AVG(Profit/Sales) AS AvgProfitMargin FROM orders GROUP BY Sub_Category",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "Which product category has the highest average quantity sold per order?",
     'SQLQuery' : "SELECT Category, AVG(Quantity) AS AvgQuantity FROM orders GROUP BY Category ORDER BY AvgQuantity DESC LIMIT 1",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "What is the total sales amount for each segment in the last quarter?",
     'SQLQuery' : "SELECT Segment, SUM(Sales) AS TotalSales FROM orders WHERE Order_Date BETWEEN '2023-10-01' AND '2023-12-31' GROUP BY Segment",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "How many unique product names are there in the orders table?",
     'SQLQuery' : "SELECT COUNT(DISTINCT Product_Name) FROM orders",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "What is the total profit earned in each region?",
     'SQLQuery' : "SELECT Region, SUM(Profit) AS TotalProfit FROM orders GROUP BY Region",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "What are the top 3 cities with the highest average sales per order?",
     'SQLQuery' : "SELECT City, SUM(Sales)/COUNT(DISTINCT Order_ID) AS AvgSalesPerOrder FROM orders GROUP BY City ORDER BY AvgSalesPerOrder DESC LIMIT 3",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "How many orders were placed by each customer?",
     'SQLQuery' : "SELECT Customer_ID, COUNT(DISTINCT Order_ID) AS TotalOrders FROM orders GROUP BY Customer_ID",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "What is the average discount rate for each product category in the western region?",
     'SQLQuery' : "SELECT o.Category, AVG(o.Discount) AS AvgDiscount FROM orders o JOIN people p ON o.Region = p.Region WHERE p.Region = 'West' GROUP BY o.Category",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "How many returns were made for each sub-category of office supplies?",
     'SQLQuery' : "SELECT o.Sub_Category, COUNT(DISTINCT r.Order_ID) AS TotalReturns FROM returns r JOIN orders o ON r.Order_ID = o.Order_ID WHERE o.Category = 'Office Supplies' GROUP BY o.Sub_Category",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "What is the total sales amount for each month in the year 2020?",
     'SQLQuery' : "SELECT EXTRACT(MONTH FROM Order_Date) AS Month, SUM(Sales) AS TotalSales  FROM orders WHERE EXTRACT(YEAR FROM Order_Date) = '2020' GROUP BY Month ORDER BY Month;",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"},
    {'Question' : "How many unique customers placed orders in each region?",
     'SQLQuery' : "SELECT Region, COUNT(DISTINCT Customer_ID) AS UniqueCustomers FROM orders GROUP BY Region",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Answer"}
]

